---
label: "Link providers"
description: "Add application-specific interactive links to structured messages or ANSI process output."
---

import structuredSource from "./structured.tsx?raw";
import ansiSource from "./ansi.tsx?raw";

## Structured messages

Link providers can detect source locations inside structured message values while the built-in HTTP/HTTPS detection continues to work.

<RunnableExample
  source={structuredSource}
  sourcePath="structured.tsx"
  title="Structured link provider"
/>

<hr />

## ANSI output

The Link providers contract works with terminal output, so source paths and application-specific tokens can be interactive in process logs too.

<RunnableExample
  source={ansiSource}
  sourcePath="ansi.tsx"
  title="ANSI link provider"
/>
