---
label: "Carriage-return progress"
description: "Step through chunked CLI progress output and see carriage-return redraws update one logical line while newline-completed stdout/stderr remains stable."
---

import Example from "./example.tsx"
import source from "./example.tsx?raw"

<RunnableExample component={Example} source={source} sourcePath="example.tsx" />
