---
label: "Resizable console"
description: "Use visible, themeable IDE-style resize separators with Lucide grip icons for vertical, horizontal, both, block, and inline resizing while keeping the console viewport scrollable."
---

<Playground />

Choose a resize direction, then drag the visible grip around the console shell. The host owns the width and height state, while the console remains scrollable inside the resized viewport. The grip cursor changes to match the active resize axis.
