---
label: "Custom renderers"
description: "Customize values and whole message rows independently while preserving built-in fallback behavior."
---

import ValueRenderer from "./value-renderer.tsx";
import valueRendererSource from "./value-renderer.tsx?raw";
import MessageRenderer from "./message-renderer.tsx";
import messageRendererSource from "./message-renderer.tsx?raw";

## Value renderer

Use a value renderer when only a particular value needs custom presentation. Returning `undefined` keeps the built-in inspector as the fallback.

<RunnableExample
  component={ValueRenderer}
  source={valueRendererSource}
  sourcePath="value-renderer.tsx"
  title="Value renderer"
/>

## Message renderer

Use a message renderer when the whole console row needs custom presentation. `renderDefault()` lets a wrapper preserve the standard message UI.

<RunnableExample
  component={MessageRenderer}
  source={messageRendererSource}
  sourcePath="message-renderer.tsx"
  title="Message renderer"
/>
