---
label: "Core Addon - Imperative scroll controls"
description: "Drive top, bottom, focus, and logical message navigation from a ConsoleHandle ref while an addon consumes the same consoleServices.viewport service."
---

import Example from "./example.tsx";
import source from "./example.tsx?raw";

<RunnableExample
  component={Example}
  source={source}
  sourcePath="example.tsx"
/>

The imperative API and the imperative-scrolling addon consume the same core viewport service. Use the buttons to call `ConsoleHandle` methods directly; the console action menu exposes top, latest-output, and focus actions from the addon.
