---
label: "Iframe console.log capture"
description: "Capture ordinary console.log calls directly from a same-origin iframe."
---

<Playground />

The iframe calls `console.log()` normally. The host captures `iframe.contentWindow.console` directly, so the iframe does not need to emit a transport event or know about the console package.
